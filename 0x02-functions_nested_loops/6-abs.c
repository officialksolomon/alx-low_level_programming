#include "main.h"

/**
 * _abs - Process and compute the absolute value of an integer
 * @n: the int to check
 * Return: the absolute value of the give int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
