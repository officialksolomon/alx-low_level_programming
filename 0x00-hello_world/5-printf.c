#include <stdio.h>

/**
 * main - Print "with proper grammar, but the outcome is a piece of art," 
 * Return: 0 for succesful run, non-zero otherwise.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0)
}
